# Address Bali Website

This folder contains the static website files for Address Bali.

## Launch

Upload the full contents of this `WEBSITE` folder to a static web server.

Start from:

```text
index.html
```

Keep these folders in place because the pages reference them with relative paths:

```text
assets/
tour/
```
